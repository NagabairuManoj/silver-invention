- hosts: test
  tasks:
         - name: Installing httpd package
           package:
              name: httpd
              state: present
         - name: starting the service
           service:
              name: httpd
              state: started
